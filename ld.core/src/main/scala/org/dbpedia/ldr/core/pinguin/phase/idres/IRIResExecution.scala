package org.dbpedia.ldr.core.pinguin.phase.idres

import org.dbpedia.ldr.core.pinguin.phase.{PhaseConfig, PhaseData, PhaseExecution}

class IRIResExecution(phaseData: PhaseData, config: PhaseConfig) extends PhaseExecution(phaseData, config) {
  override def execute(input: PhaseData): PhaseData = null
}
